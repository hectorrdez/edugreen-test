info:
  name: Update info a user
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{API_DOMAIN}}:{{API_PORT}}/api/user/{{USER_ID}}"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "name":"test",
        "lastName":"admin",
        "email":"test.admin@edugreen"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test(`Response with expected status`, () => {
          expect(res.getStatus()).to.be.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
