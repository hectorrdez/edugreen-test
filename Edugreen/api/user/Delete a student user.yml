info:
  name: "Delete a student user "
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{API_DOMAIN}}:{{API_PORT}}/api/user/{{STUDENT_USER_ID}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test(`Response with expected status`, () => {
          expect(res.getStatus()).to.be.equal(200);
        });

        test(`Deleted successfully`, () => {
          expect(res.getBody().data.message).to.be.equal("User deleted successfully");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
