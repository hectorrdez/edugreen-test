info:
  name: Get all enrollments by user_id
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_DOMAIN}}:{{API_PORT}}/api/enrollment/user/{{USER_ID}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test(`Response with expected status`, () => {
          expect(res.getStatus()).to.be.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
