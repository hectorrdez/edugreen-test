info:
  name: api
  type: folder
  seq: 1

request:
  auth:
    type: apikey
    key: Authorization
    value: "{{API_KEY}}"
    placement: header
  scripts:
    - type: tests
      code: |-
        test(`Response is a JSON`, () =>{
          expect(res.getBody());
        });
